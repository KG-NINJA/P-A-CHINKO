# Anime Pachinko

GitHub Pages向けの公開フォルダです。

## 公開

1. `github-pages` フォルダ内のファイルをGitHubリポジトリのルートへ配置
2. GitHubの `Settings > Pages` を開く
3. `Deploy from a branch` を選択
4. 公開ブランチの `/ (root)` を選択

`index.html` のみでゲームが動作します。

